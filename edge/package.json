{
  "name": "treefrog-edge",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Pages for Tree Frog Streams",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.3",
    "wrangler": "^4.100.0"
  }
}
