# Hard resource caps so the engine coexists with AIOstreams on the same host.
# See plan.md §12.1 for the reasoning.
services:
  tf-engine:
    build: .
    image: treefrog-engine:dev
    container_name: treefrog-engine
    restart: unless-stopped
    cpus: 0.5                 # hard cap, AIOstreams must not be starved
    mem_limit: 1g             # hard cap
    mem_reservation: 512m     # guaranteed floor
    pids_limit: 200           # fork-bomb guard
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    # Bind the admin API to 127.0.0.1 on the HOST only. It's still not
    # reachable from the public internet — only from the VPS host
    # itself, and from Tailscale devices that route to this host's
    # Tailscale IP. No other interface is exposed.
    #
    # If you don't need Tailscale access (e.g. you do everything from
    # the VPS host via `docker exec`), comment this block out.
    ports:
      - "127.0.0.1:8000:8000"
